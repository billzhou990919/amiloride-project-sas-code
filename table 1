proc freq data=cms.study_cohort_v2;
    tables pd * amiloride;
run;

proc freq data=cms.study_cohort_v2;
    tables gndr_cd * amiloride;
run;

proc freq data=cms.study_cohort_v2;
    tables age * amiloride;
run;

proc freq data=cms.study_cohort_v2;
    tables change_detected * amiloride;
run;

proc freq data=cms.study_cohort_v2;
    var gnn;
run;

